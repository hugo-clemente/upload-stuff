// Tracked substitute for the gitignored, generated `next-env.d.ts` so lint's
// type-aware pass works on fresh checkouts. Keep in sync with what
// `next typegen` emits.
/// <reference types="next" />
/// <reference types="next/image-types/global" />
