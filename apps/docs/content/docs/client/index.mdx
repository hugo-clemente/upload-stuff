---
title: "Client engine"
description: "Use the framework-free upload client for uploads, route config, progress, aborts, and headers."
---

import { Callout } from 'fumadocs-ui/components/callout';

This page will cover `createUploadStuffClient({ baseURL, basePath? })`, `uploadFiles(endpoint, files, options)`, route input via `options.input`, callbacks, `headers`, `signal`, `uploadProgressGranularity`, `getRouteConfig(endpoint, { force? })`, and `preprocessImages`.

<Callout type="warn" title="Page in progress">
This page is planned but not yet written.
</Callout>
