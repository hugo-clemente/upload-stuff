---
title: "Cleanup job"
description: "Sweep expired never-completed uploads on a schedule."
---

import { Callout } from 'fumadocs-ui/components/callout';

This guide will cover running `serverUtils.cleanUpFiles()` on a schedule to remove pending rows and orphaned objects left by uploads that were initialized but never completed.

<Callout type="warn" title="Page in progress">
This page is planned but not yet written.
</Callout>
