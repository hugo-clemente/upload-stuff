---
title: "File router"
description: "Define typed upload routes with file constraints, input schemas, middleware, fields, and completion callbacks."
---

import { Callout } from 'fumadocs-ui/components/callout';

This page will cover `createUploadStuffRouter`, the `RouteConfig` shape (`isPublic`, `files`, `maxFileSize`, `maxFileCount`), per-type constraints, and the builder chain `.input()`, `.middleware()`, `.fields()`, `.onUploadComplete()`, and `.build()`.

<Callout type="warn" title="Page in progress">
This page is planned but not yet written.
</Callout>
