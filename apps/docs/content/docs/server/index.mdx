---
title: "Instance configuration"
description: "Configure the UploadStuff instance, adapters, fields, upload windows, defaults, and server utilities."
---

import { Callout } from 'fumadocs-ui/components/callout';

This page will cover every `UploadStuff(...)` option: `storageAdapter`, `databaseAdapter`, `filePublicUrlGenerator`, `fileIdGenerator`, `fileKeyGenerator`, `fields`, `uploadWindowSeconds`, `defaultMaxFileCount`, and `defaultMaxFileSize`, plus `serverUtils.cleanUpFiles()`, `serverUtils.uploadFile({ data, content })`, and `serverUtils.deleteFiles(fileIds)`.

<Callout type="warn" title="Page in progress">
This page is planned but not yet written.
</Callout>
