node_modules
dist
coverage
*.tsbuildinfo
.DS_Store
.next
.source
.vercel
out
next-env.d.ts

# Local planning docs (brainstorms/plans/specs) — kept locally, not tracked.
# Anchored to root so apps/docs (the docs site) stays tracked.
/docs/
