{
  "name": "upload-stuff-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vp run -F './packages/*' build",
    "dev": "vp run -r --parallel dev",
    "check-types": "vp run -F './packages/*' check-types",
    "lint": "vp lint .",
    "test": "vp test run",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "vp run -F './packages/*' build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.10",
    "@vitest/coverage-v8": "^4.1.9",
    "typescript": "5.9.3",
    "vite-plus": "^0.1.22"
  },
  "packageManager": "pnpm@9.15.9"
}
